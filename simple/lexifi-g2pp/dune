;; Adapted from OCamlPro's ocamlbench-repo: https://github.com/OCamlPro/ocamlbench-repo
(executable
 (name main)
 (modes native byte))

(alias (name buildbench) (deps main.exe))

(alias (name bytebench) (deps main.bc))